# Cognix Server

Express API server for Cognix, the AI prompt sharing and marketplace platform.

## Getting Started

```bash
cp .env.example .env
npm install
npm run dev
```

The API runs on `http://localhost:5000` by default.

## Health Check

```bash
GET /api/health
```
