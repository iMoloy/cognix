{
  "name": "cognix-server",
  "version": "1.0.0",
  "private": true,
  "description": "Express API server for the Cognix prompt marketplace.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=20.6.0"
  }
}
